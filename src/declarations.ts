export interface AppPage {
  url: string;
  icon: object;
  title: string;
}

export interface AvisosItem {
  srcImage: string;
  icone: object;
  titulo: string;
  descricao: string;
  preco: DoubleRange;
}